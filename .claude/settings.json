{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -re '.tool_input.command | test(\"git commit\")' > /dev/null && git push origin HEAD 2>&1 || true",
            "statusMessage": "Pushing to GitHub..."
          }
        ]
      }
    ]
  }
}
